const darkBodyColor = "#000";
const darkTextColor = "#fff";

const lightBodyColor = "#fff";
const lightTextColor = "#000";

const lightColorSwitch = "#757575";
const darkColorSwitch = "#a644ee";

const lightStrokeLogo = "#838383";
const darkStrokeLogo = "#ffffff";

export const lightTheme = {
  primaryColor: "#A445ED",
  bodyColor: lightBodyColor,
  textColor: lightTextColor,
  strokeLogo: lightStrokeLogo,
  colorSwitch: lightColorSwitch,
};

export const darkTheme = {
  primaryColor: "#A445ED",
  bodyColor: darkBodyColor,
  textColor: darkTextColor,
  strokeLogo: darkStrokeLogo,
  colorSwitch: darkColorSwitch,
};
